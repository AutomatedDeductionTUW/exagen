% Random number generator seed: Just 123
% Index: 9
% Substitution: {x -> d,y -> h(b)}
\prftree{P ( h ( h ( b ) ) ) \lor  \lnot P ( g ( g ( d , c ) , b ) ) \lor g ( g ( d , d ) , d ) = g ( d , h ( b ) )}{ \lnot P ( h ( y ) ) \lor g ( g ( x , x ) , x ) = g ( d , h ( b ) )}{ \lnot P ( g ( g ( d , c ) , b ) ) \lor g ( g ( d , d ) , d ) = g ( d , h ( b ) )}
